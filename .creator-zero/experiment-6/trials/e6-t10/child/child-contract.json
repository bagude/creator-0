{
  "contract_id": "K-E6-child-e6-t10",
  "version": "0.1-e6",
  "parent_contract": "K-E6-trial",
  "envelope_ceiling": "K-E6-child-max",
  "description": "Terminal worker contract for the isolated fresh child of trial e6-t10. Attenuated from K-E6-trial on every governed axis and within the K-E6-child-max ceiling. The child performs one systematic, independent search over the cart domain and returns a deliverable; it may not create, delegate, realize, or launch anything.",
  "allowed_primitives": ["observe", "hypothesize", "verify", "return"],
  "allowed_relations": ["observe", "verify", "authorize", "return"],
  "allowed_tools": ["Read", "Write"],
  "max_children": 0,
  "max_depth": 0,
  "max_model_calls": 1,
  "may_create_creator": false,
  "may_realize_creation": false,
  "required_primitives_for_mutation": ["verify"],
  "protected_rules": [
    "No child may increase its authority envelope.",
    "Model output is a proposal, never authorization.",
    "Mutation-capable topology requires verification.",
    "The child is terminal: it may not create, delegate, or launch anything."
  ]
}
